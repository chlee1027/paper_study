# 글 쓰는 규칙

랩 문서를 쓰면서 지적받아 세운 규칙들이다. 여기 노트에도 같은 규칙을 쓴다 — **두 곳에서 다른 습관을 들이면 랩 문서를 쓸 때 다시 지적받는다.**

## 1. 측정한 것보다 강하게 쓰지 않는다

관찰한 범위를 그보다 센 이름으로 부르지 않는다.

| 쓰지 않는다 | 대신 |
|---|---|
| 대가가 0이다 | 관찰한 범위가 0.000에서 0.164 백분율 점이다 |
| 구조적으로 불가능하다 | 훑은 범위(인용 108건)에서 나오지 않았다 |
| 판정 한계 안이다 | (판정 한계를 먼저 정의하고) 그 한계가 0.5 백분율 점이고 관찰값이 0.18이다 |
| A가 B보다 낫다 | seed 셋의 변동 폭이 0.12인데 차이가 0.04라 순서를 매기지 않는다 |

**"없다"라고 적지 않는다.** 찾은 경로와 범위를 적고 그 범위에서 나오지 않았다고 적는다.

## 2. 기준이 상대적인 말을 쓰지 않는다

`근처` · `거의` · `넉넉히` · `무시할 수준` · `충분히` · `대략` 같은 말은 읽는 사람이 기준을 모른다. **측정값으로 바꾼다.**

| 쓰지 않는다 | 대신 |
|---|---|
| 거의 손실이 없다 | 손실 0.005 백분율 점 |
| 넉넉히 들어간다 | 장치 천장의 9% |
| 무시할 수준 | 0.1% |
| 3분의 1 아래 | 23.0에서 33.3% (그리고 **무엇의** 3분의 1인지 분모를 적는다) |

**배수와 비율을 적을 때는 분모를 함께 적는다.** 단위가 갈릴 수 있으면 어느 단위로 센 것인지도 적는다(GB와 GiB, 벡터 수와 바이트).

## 3. 문서 하나로 읽히게 쓴다

- **다른 문서를 지칭하지 않는다.** "지난주 브리프 참고"라고 적지 않고 필요한 내용을 여기에 다시 적는다.
- **경로는 문서 끝 한 곳에 모은다.** 본문 중간에 파일 경로를 흩어 놓지 않는다.
- **서론은 그 용어를 모르는 첫 독자를 기준으로 쓴다.** 줄임말은 처음 나올 때 풀어 적는다.

## 4. 한 낱말이 여러 뜻이면 버린다

같은 것을 두 이름으로 부르지 않고, 한 이름이 두 뜻을 갖게 두지 않는다. 노트마다 **「용어」 절을 먼저 두고** 그 노트에서 쓸 이름을 정한다. 논문의 원래 용어는 괄호로 함께 적는다.

## 5. 인용을 가른다

- **원리는 원 출처**를 인용한다.
- **구현은 그 라이브러리의 논문**을 인용한다.

예시로, 곱 양자화를 쓴다면 원리는 그 방법을 낸 논문을, 실제로 부른 것이 어떤 라이브러리라면 그 라이브러리 논문을 함께 적는다.

## 6. 이모지와 장식 기호를 쓰지 않는다

이모지, 체크 표시, 별표 같은 장식 기호를 문서에 넣지 않는다. 예외 없다. 강조는 **굵게**와 표와 평문 라벨로 한다.

## 7. 설명에 작은 예시를 붙인다

정의만 적으면 읽히지 않는다. **정의 한 줄 뒤에 숫자가 든 작은 예시 한 줄**을 붙인다.

> 예시: **최대 파티션 비율**은 가장 큰 파티션의 벡터 수를 뱅크 전체 벡터 수로 나눈 값이다. 작은 예시로, 뱅크가 1,336,000개이고 가장 큰 파티션이 113,486개면 0.085다.
